pipeline {
    agent any
    stages {
        stage('Build project with tag') {
            steps{
                git credentialsId: "test_Node", branch: "main" url:'git@github.com:Serhikozak/NODE.git'

            }
        }
    }
}
